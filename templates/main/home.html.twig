{% extends 'base.html.twig' %}

{% block body %}
{% if filterForm %}

    {% form_theme filterForm 'foundation_5_layout.html.twig' %}
    {{ form_start(filterForm) }}
    {{ form_widget(filterForm) }}
        <button>Submit</button>
    {{ form_end(filterForm) }}
 {% endif %}

 {{ include('inc/event.html.twig') }}

    <a href="{{ path('activity_create') }}" title=""><button class="btn btn-primary"></a>Créer une sortie</button>
{% endblock %}

{% block title %}

{% endblock %}
